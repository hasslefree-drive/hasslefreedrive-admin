{
  "name": "hasslefreedrive-admin",
  "version": "1.0.0",
  "description": "HassleFreeDrive Admin Panel - Fullstack",
  "scripts": {
    "dev": "concurrently \"npm run dev --silent --prefix frontend\" \"npm run dev --silent --prefix backend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "seed:admin": "npm run seed:admin --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}